import 'package:flutter/material.dart';

class DashboardKP extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('KP Dashboard Placeholder')),
    );
  }
}
